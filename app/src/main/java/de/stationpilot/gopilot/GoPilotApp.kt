package de.stationpilot.gopilot

import android.app.Application

class GoPilotApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Globale Initialisierung (z.B. Logging, Crash-Reporting)
    }
}
